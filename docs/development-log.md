# 开发日志

记录实际开发时间，不倒填项目经历。

## 2026-09-16 15:28–16:15（Asia/Shanghai）：基线阶段

搭建解析、混合检索、可恢复流程和离线测试；运行BGE语义基线与合成数据实验。此阶段产生的评估和负载JSON属于原型研究，不代表当前产品指标。

## 2026-09-16 20:21：交付标准调整

用户明确要求可上线、非Demo；面试材料单独交付，不出现在产品导航。停止把静态演示页当成最终交付，重新按真实身份、数据、模型、持久任务、审批和审计设计。

## 2026-09-16 20:40–21:16：生产链路与验收测试

增加PostgreSQL模型、服务端授权、随机会话和Argon2密码、数据库队列与Worker租约。完成产品API和登录后操作界面，删除web目录中的模拟数据及模拟逻辑。固定初始数据库迁移SQL；增加Docker Compose与Caddy配置。

测试时模型/向量适配器采用明确标记的测试替身，检验契约和隔离，不冒充真实模型效果。本地完整测试36项通过，其中生产平台和真实导入/图执行/审批链路8项。 这组结果尚未证明PostgreSQL并发锁与公开部署，后续CI单独验证。

本地Docker客户端存在，但Docker daemon不可连接，不能据此称容器已在本机启动。已安排CI使用真实PostgreSQL服务、容器构建和API/Worker就绪检查。

## 待完成的部署验收

真实后端主机/域名、模型配置、数据外发许可、实际模型业务用例、备份恢复演练、生产容量与告警策略。缺少部署资源时不编造上线时间。

## 2026-09-16 21:24–21:26：真实 CI 与前端发布

GitHub Actions 已通过 PostgreSQL 集成测试（包含并发认领）、固定迁移、Docker 镜像构建和 API/Worker 就绪检查。本次37项测试通过，Pages发布完成于21:26:05。 证据：[首次完整验收](https://github.com/xiaoguos/business-insight-agent/actions/runs/35101674296)。只发布了静态前端，没有自动获得公网后端或模型账号。

## 2026-09-17 02:03–02:15：浏览器验收与截图

访问线上登录页，连接本地隔离后端，验证真实导入、状态查询、审计与390px移动端布局，无浏览器JavaScript错误。知识平台实际调用本地BGE索引验收文档；分析平台导入明确标识的合成验收CSV。未配置生成模型时验证明确失败，不合成答案或报告截图。

修复问答失败后仍显示加载中的界面问题，以及SQLite隔离环境时间缺少时区导致的显示偏差。通过格式化与静态检查改善可维护性；自动清理未使用导入曾移除pytest隐式fixture导入，回归测试立即检出，改为conftest显式导出后恢复通过。

截图目录中的phase1文件是旧版历史；production-login为真实Pages登录截图，workspace为本地后端验收截图，不能当成公网模型链路已完成的证明。

## 2026-09-17 02:16–02:30：补齐真正多 Agent 编排

用户确认第二个项目必须实现多Agent，并再次强调成品和需求闭环。此前受控工作流不等同于多Agent，已明确说明差距，没有仅改节点名称。

增加Conductor、Analysis、Knowledge、Report四个独立角色及各自决策循环、严格消息协议、能力白名单、独立上下文和持久AgentRun。LangGraph将分析与知识检索并行，显式汇合后执行报告Agent。增加BusinessRule不可变快照及0002迁移，前端支持规则导入、选择知识范围、Agent能力页面和逐任务执行轨迹。

增加用户require_rules硬策略，模型不能放宽；加入真实模型闭环验收脚本，要求明确写入授权和人工审批确认。当前自动化模型替身仅用于tests，不导入生产入口。

本地第一轮多Agent专项及既有审批回归12项通过；整套回归随后44项通过、1项PostgreSQL锁测试跳过。新增用户硬策略测试与最终PostgreSQL CI结果以后续验收记录为准，未在此预填成功。

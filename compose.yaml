services:
  api:
    build: .
    ports:
      - "127.0.0.1:8002:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - runtime:/app/data/runtime
    restart: unless-stopped
volumes:
  runtime:

